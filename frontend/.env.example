VITE_PAYSTACK_PUBLIC_KEY=pk_test_xxx

# Full URL of the deployed backend API, including the /api prefix, e.g.
# https://your-api.onrender.com/api. Leave unset for local dev — Vite's
# dev server proxies relative "/api" requests to the backend instead
# (see vite.config.js).
VITE_API_URL=
